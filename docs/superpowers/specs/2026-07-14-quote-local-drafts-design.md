# 本機多草稿

## 目的

讓報價單產生器可在同一瀏覽器保存、切換與管理多份草稿，不需登入或伺服器。

## 方案

- 以瀏覽器 `localStorage` 保存一個草稿清單與目前草稿 ID。
- 每份草稿保存表單欄位、合作明細、名稱與更新時間。
- 編輯時自動保存；可新增、改名、切換與刪除草稿。
- 始終保留至少一份草稿；清除功能僅刪除目前草稿。

## 限制

資料僅存於目前瀏覽器；清除網站資料、使用無痕模式或換裝置時不會保留。

## 驗證

以小型 Node 測試確認草稿建立、切換與序列化；在頁面確認重整後草稿仍會載入。
