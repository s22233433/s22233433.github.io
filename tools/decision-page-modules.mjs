import { caseEnrichment } from './case-enrichment-content.mjs';
import { decisionSupport } from './decision-support-content.mjs';

const esc = value => String(value).replace(/[&<>"']/g, c => ({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const list = items => `<ul>${items.map(x=>`<li>${esc(x)}</li>`).join('')}</ul>`;
const annotationLabel = title => title.replace(/^\d+\s*[｜|]\s*/, '');
const table = (caption, columns, rows) => `<div class="decision-table" role="region" aria-label="${esc(caption)}" tabindex="0"><table><caption>${esc(caption)}</caption><thead><tr>${columns.map(x=>`<th scope="col">${esc(x)}</th>`).join('')}</tr></thead><tbody>${rows.map(row=>`<tr>${row.map((x,i)=>i?`<td>${esc(x)}</td>`:`<th scope="row">${esc(x)}</th>`).join('')}</tr>`).join('')}</tbody></table></div>`;
const labels = {
 'zh-Hant':{context:'專案背景與任務',details:'完整專案摘要',copy:'複製詢價清單',copied:'已複製，可貼到合作需求的補充欄位。',fallback:'已選取清單，請使用系統的複製功能。',caseNotes:'案例素材與內容判讀',scenario:['合作目標','常見交付','費用變因'],handover:['項目','核對重點']},
 'zh-Hans':{context:'项目背景与任务',details:'完整项目摘要',copy:'复制询价清单',copied:'已复制，可粘贴到合作需求的补充字段。',fallback:'已选中清单，请使用系统的复制功能。',caseNotes:'案例素材与内容解读',scenario:['合作目标','常见交付','费用因素'],handover:['项目','核对重点']},
 en:{context:'The brief and the work',details:'Full project outline',copy:'Copy the inquiry checklist',copied:'Copied. Paste it into the additional details field of your inquiry.',fallback:'Checklist selected. Use your device’s copy command.',caseNotes:'Case asset and editorial reading',scenario:['Objective','Typical deliverables','Cost drivers'],handover:['Item','What to check']}
};

export function caseHero(study,locale,assetUrl){
 const d=caseEnrichment[study.slug][locale.key],ui=labels[locale.key];
 return `<div class="case-opening"><div><span class="eyebrow">CASE STUDY / ${esc(d.eyebrow)}</span><h1>${esc(study.title[locale.key])}</h1><p class="case-summary">${esc(study.summary[locale.key])}</p><dl class="case-facts">${d.summaryLabels.map((x,i)=>`<div><dt>${esc(x)}</dt><dd>${esc(d.summaryValues[i])}</dd></div>`).join('')}</dl></div><figure class="case-asset"><img src="${esc(assetUrl)}" alt="${esc(study.title[locale.key])}" width="${study.slug==='liming-weiquan-cheer'?1776:1180}" height="${study.slug==='liming-weiquan-cheer'?1340:study.slug==='camay-curling-iron'?1024:1043}" fetchpriority="high"><figcaption>${esc(d.visualCaption)}</figcaption></figure></div><div class="asset-reading" aria-label="${esc(ui.caseNotes)}"><h2>${esc(d.annotationTitle)}</h2><div class="reading-grid">${d.annotations.map(([title,text],i)=>`<article><span class="step-no">0${i+1}</span><h3>${esc(annotationLabel(title))}</h3><p>${esc(text)}</p></article>`).join('')}</div></div>`;
}
export function caseDecisions(study,locale){
 const d=caseEnrichment[study.slug][locale.key];
 return `<section class="case-reasoning"><div class="wrap"><span class="eyebrow">DECISIONS / EXECUTION</span><h2>${esc(d.decisionTitle)}</h2><div class="reasoning-list">${d.decisions.map(([question,rationale],i)=>`<article><span class="step-no">0${i+1}</span><div><h3>${esc(question)}</h3><p>${esc(rationale)}</p></div></article>`).join('')}</div></div></section>`;
}
export function caseHandover(study,locale){
 const d=caseEnrichment[study.slug][locale.key],ui=labels[locale.key];
 return `<section class="case-handover"><div class="wrap"><h2>${esc(d.handoverTitle)}</h2><p class="module-intro">${esc(d.handoverIntro)}</p>${table(d.handoverTitle,ui.handover,d.handover)}<div class="case-boundary"><div><h3>${esc(d.applicationTitle)}</h3><p>${esc(d.application)}</p></div><div><h3>${esc(d.boundaryTitle)}</h3><p>${esc(d.boundary)}</p></div></div></div></section>`;
}
export function taiwanDecisionModules(locale){
 const d=decisionSupport[locale.key].service;
 return `<section class="scope-module"><div class="wrap"><span class="eyebrow">WORKING TOGETHER</span><h2>${esc(d.scopeTitle)}</h2><p class="module-intro">${esc(d.scopeIntro)}</p><div class="scope-columns">${d.columns.map((c,i)=>`<article><span class="step-no">0${i+1}</span><h3>${esc(c.title)}</h3>${list(c.items)}</article>`).join('')}</div></div></section><section class="sample-module"><div class="wrap"><div class="sample-heading"><h2>${esc(d.sampleTitle)}</h2><span class="sample-tag">${locale.key==='en'?'ILLUSTRATIVE SAMPLE':locale.key==='zh-Hans'?'虚构流程示意':'虛構流程示意'}</span></div><p class="module-intro">${esc(d.sampleDisclosure)}</p>${table(d.sampleTitle,d.sampleColumns,d.sampleRows)}<p class="module-note">${esc(d.sampleNote)}</p></div></section>`;
}
export function pricingDecisionModules(locale){
 const d=decisionSupport[locale.key].pricing,ui=labels[locale.key];
 return `<section class="pricing-comparison"><div class="wrap"><span class="eyebrow">COMPARE THE SCOPE</span><h2>${esc(d.comparisonTitle)}</h2><p class="module-intro">${esc(d.comparisonIntro)}</p>${table(d.comparisonTitle,d.columns,d.rows)}</div></section><section class="pricing-scenarios"><div class="wrap"><h2>${esc(d.scenarioTitle)}</h2><p class="module-intro">${esc(d.scenarioIntro)}</p><div class="scope-columns">${d.scenarios.map((s,i)=>`<article><span class="step-no">0${i+1}</span><h3>${esc(s.title)}</h3><dl>${['goal','deliverables','costDrivers'].map((k,j)=>`<dt>${esc(ui.scenario[j])}</dt><dd>${esc(s[k])}</dd>`).join('')}</dl></article>`).join('')}</div></div></section><section class="pricing-request"><div class="wrap"><h2>${esc(d.requestTitle)}</h2><p class="module-intro">${esc(d.requestIntro)}</p><label class="sr-only" for="quote-checklist">${esc(d.requestTitle)}</label><textarea id="quote-checklist" readonly rows="12">${esc(d.requestTemplate)}</textarea><div class="actions"><button type="button" class="button" data-copy-inquiry data-track-event="article_cta_click" data-track-location="pricing-copy-brief" data-copy-success="${esc(ui.copied)}" data-copy-fallback="${esc(ui.fallback)}">${esc(ui.copy)}</button><p role="status" aria-live="polite" data-copy-status></p></div></div></section>`;
}
export function historicalPricing(locale,html){
 const d=decisionSupport[locale.key].pricing;
 return `<section class="historical-pricing"><div class="wrap"><h2>${esc(d.historyTitle)}</h2><p class="module-intro">${esc(d.historyNote)}</p><details><summary>${locale.key==='en'?'Read the limited historical reference':locale.key==='zh-Hans'?'展开历史参考及其限制':'展開歷史參考及其限制'}</summary><div class="grid">${html}</div></details></div></section>`;
}

export const decisionCss = `
 .module-intro{max-width:850px;color:var(--muted);line-height:1.8}.module-note{font-size:14px;color:var(--muted)}
 .step-no{font:italic 30px Georgia,serif;color:var(--coral)}.scope-columns,.reading-grid{display:grid;grid-template-columns:repeat(3,minmax(0,1fr));gap:28px}
 .scope-columns>article{border-top:2px solid var(--coral);padding:24px 0}.scope-columns h3,.reading-grid h3{font-size:22px;line-height:1.4;margin:12px 0}.scope-columns ul{padding-left:20px;color:var(--muted)}.scope-columns li+li{margin-top:12px}
 .scope-columns dt{font-size:13px;font-weight:800;margin-top:20px;color:var(--ink)}.scope-columns dd{margin:5px 0 0;color:var(--muted);line-height:1.7}
 .sample-module,.case-reasoning,.pricing-scenarios{background:#f3f0e8}.sample-heading{display:flex;align-items:center;justify-content:space-between;gap:20px;flex-wrap:wrap}.sample-tag{border:1px solid #68766c;padding:6px 12px;color:#405446;font-size:12px;letter-spacing:.07em}
 .decision-table{overflow-x:auto;background:#fff;border:1px solid var(--line);margin:24px 0}.decision-table table{width:100%;min-width:600px;border-collapse:collapse;text-align:left}.decision-table caption{text-align:left;padding:16px 20px;font-weight:800;color:var(--ink)}.decision-table th,.decision-table td{padding:18px 20px;line-height:1.7;vertical-align:top;border-bottom:1px solid var(--line)}.decision-table thead th{background:#20332d;color:#fff;font-size:14px}.decision-table tbody th{background:#f8f6ef;color:var(--ink);width:22%;font-size:15px}.decision-table td{color:var(--muted)}.decision-table tr:last-child>*{border-bottom:0}
 .case-opening{display:grid;grid-template-columns:minmax(0,.85fr) minmax(0,1.15fr);gap:48px;align-items:start}.case-opening h1{font-size:clamp(36px,4vw,60px);line-height:1.16}.case-facts{margin:28px 0 0;border-top:1px solid var(--line)}.case-facts>div{display:grid;grid-template-columns:90px 1fr;gap:20px;border-bottom:1px solid var(--line);padding:16px 0}.case-facts dt{color:var(--muted);font-size:13px}.case-facts dd{margin:0;font-size:16px;font-weight:700}
 .case-asset{margin:0;min-width:0}.case-asset img{display:block;width:100%;height:auto;border:1px solid var(--line)}.case-asset figcaption{font-size:12px;line-height:1.65;color:var(--muted);padding-top:12px}.asset-reading{border-top:1px solid var(--line);margin-top:48px;padding-top:24px}.asset-reading h2{font-size:26px}.reading-grid article{border-left:2px solid #c7cdbf;padding-left:20px}.reading-grid p{color:var(--muted);font-size:15px;line-height:1.8}
 .reasoning-list{max-width:950px}.reasoning-list article{display:grid;grid-template-columns:60px 1fr;gap:20px;border-top:1px solid #ced3c9;padding:24px 0}.reasoning-list h3{font-size:22px;margin:0 0 10px}.reasoning-list p{color:var(--muted);margin:0;line-height:1.8}.case-boundary{display:grid;grid-template-columns:1fr 1fr;gap:32px;margin-top:32px}.case-boundary>div{border-left:3px solid var(--teal);padding:0 24px}.case-boundary h3{margin:0 0 12px}.case-boundary p{margin:0;color:var(--muted);line-height:1.8}
 .pricing-request textarea{width:100%;max-width:900px;padding:24px;font-family:inherit;font-size:16px;line-height:1.8;color:var(--ink);background:#fff;border:1px solid var(--line);border-radius:4px;resize:vertical}.pricing-request button{font:inherit;cursor:pointer}.pricing-request [role=status]{font-size:14px;color:var(--muted)}.historical-pricing{background:#f6f4ef}.historical-pricing summary,.service-notes summary{cursor:pointer;font-weight:800;font-size:20px;padding:16px 0}.historical-pricing .grid,.service-notes .grid{margin-top:16px}
 .sr-only{position:absolute;width:1px;height:1px;padding:0;margin:-1px;overflow:hidden;clip:rect(0,0,0,0);white-space:nowrap;border:0}.decision-table:focus-visible,summary:focus-visible,button:focus-visible,textarea:focus-visible{outline:3px solid var(--teal);outline-offset:4px}
 @media(max-width:760px){.case-opening,.scope-columns,.reading-grid,.case-boundary{grid-template-columns:1fr}.case-opening{gap:18px}.case-opening>div{display:contents}.case-opening .eyebrow{order:0}.case-opening h1{order:1;font-size:36px;margin:0}.case-opening .case-summary{order:2;margin:0}.case-opening .case-asset{order:3}.case-opening .case-facts{order:4;margin-top:0}.scope-columns,.reading-grid{gap:16px}.asset-reading{margin-top:30px}.case-facts>div{grid-template-columns:80px 1fr}.decision-table th,.decision-table td{padding:14px}.case-boundary>div{padding-left:18px}.reasoning-list article{grid-template-columns:38px 1fr;gap:12px}.pricing-request textarea{padding:16px;font-size:16px}html[lang=en] .case-opening h1{font-size:32px}}
`;
